# Web Framework
Flask==3.0.0
Flask-Login==0.6.3
Flask-WTF==1.2.1
WTForms==3.1.1

# Database
SQLAlchemy==2.0.23
Flask-SQLAlchemy==3.1.1
# Pure-Python MySQL driver for the deploy-time migration scripts (the deploy
# venv has no DB driver, and mysqlclient can't build there). The migrations'
# _dbapp helper translates the prod URL's ssl-mode param into an SSL context.
PyMySQL==1.1.0

# HTTP client (kept for outbound API calls)
requests==2.31.0

# Slide watermarking
Pillow==12.1.0

# Excel Export
openpyxl==3.1.2

# Scheduling
APScheduler==3.10.4

# Security
python-dotenv==1.0.0
Werkzeug==3.0.1

# Email (transactional verification only)
resend==2.23.0

# Testing
pytest==7.4.3
pytest-cov==4.1.0
pytest-flask==1.3.0
